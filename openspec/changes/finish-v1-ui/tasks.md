# Tasks: Finish V1 UI

- [x] Instalar e configurar dependências de UI adicionais (`@react-native-community/datetimepicker`, `expo-notifications`).
- [x] Atualizar `ProfileScreen.tsx` vinculando `onPress` ao `useSync().triggerSync`.
- [x] Adicionar lógica de datas (start/end date) com DateTimePicker no `ReportGeneratorScreen.tsx`.
- [x] Modificar `DashboardScreen.tsx` para carregar e listar componentes dinâmicos de inspeções recentes vindas do repositório SQLite.
- [x] Configurar `expo-notifications` (app.json / permissões base) e remover hardcode do `NotificationsScreen.tsx`.
